# Alan's Calculator

A small, browser-based version of the original C++ calculator. It supports addition, subtraction, multiplication, division, and integer modulus for two input values.

## Run locally

The project is a dependency-free static site. Start it with:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

Then open the Replit preview.